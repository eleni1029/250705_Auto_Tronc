1_folder_merger將googledrive下載解壓文件從projects合併到merged_projects
2_scorm_packager_從merged_projects中找到xml封裝為scorm_packages
3_manifest_extractor_從merged_projects中找到xml抽取待處理結構文件manifest_structures
4_資源庫路徑_補充
4_cloud_mapping_根據mapping文件生成待補充資源庫路徑的excel
5_0_to_be_executed_excel_generator_生成待執行文件
6_system_todolist_maker_生成系統批次執行文件
7_start_tronc_開始自動創課






0. 虛擬環境
source venv/bin/activate

a. GIT 連接
git init
git remote add origin https://github.com/eleni1029/250627_Dynamic_Form_System.git

b. 把內容都拉下來
git fetch origin
git reset --hard origin/main

c. 更新到 git 上
git add .
git commit -m "常規更新"
git push -u origin main

c. 強制更新到 git 上
git add .
git commit -m "常規更新"
git push origin main --force


-
啟動 Docker
docker-compose up --build

前端依賴與運行
cd frontend
npm install
npm run dev

後端依賴與運行
cd backend
npm install
npm run dev



--
# 連接到 PostgreSQL
psql postgres

# 在 psql 中執行
CREATE DATABASE dynamic_form_system;
CREATE USER postgres WITH PASSWORD 'postgres123';
GRANT ALL PRIVILEGES ON DATABASE dynamic_form_system TO postgres;
\q